#include <stdio.h>

int * find_missing_num1(int *a, int n) {
    int *new_arr = (int *)malloc(sizeof(int)*n);   

    return new_arr;
}

int main() {
    //int *a = (int *)malloc(sizeof(int)*10);
    //free(a);
    int lost_a, lost_b;
    a[] = {3,1,9,8,2,4,8,5,7,0};
    int *b = find_missing_num1(a, sizeof(a)/sizeof(int));
    int i = 0;
    for(i = 0; i < n; i++) {
             
    }
    free(b);
    return 0;
}
